import Notifications from "../../components/userPages/settings/Notifications";

const SettingsPage = () => {
    return (
        <div>
            <Notifications />
        </div>
    );
};

export default SettingsPage;
