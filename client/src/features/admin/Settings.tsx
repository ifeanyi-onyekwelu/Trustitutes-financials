import Notifications from "../../components/userPages/settings/Notifications";

const Settings = () => {
    return (
        <div>
            <Notifications />
        </div>
    );
};

export default Settings;
